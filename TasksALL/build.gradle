plugins {
    id 'java'
  }

version = '1.0.0'

repositories {
    mavenCentral()
}

dependencies {
    //более новые версии не загружаются, команды compile не существует, есть только compileClassPath
    implementation('org.testng:testng:7.3.0')
    implementation 'org.apache.commons:commons-math3:3.6.1'
}

